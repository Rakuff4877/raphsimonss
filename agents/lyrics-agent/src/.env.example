# Option A — single combined key (format: apikey:apisecret)
# HF_KEY=your-api-key:your-api-secret

# Option B — separate key and secret (both required)
HF_API_KEY=your-api-key-here
HF_API_SECRET=your-api-secret-here
